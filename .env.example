# PostHog analytics. Copy to .env.local and fill in.
# Project API key from PostHog > Settings > Project.
NEXT_PUBLIC_POSTHOG_KEY=
# Reverse-proxy path (see next.config.js rewrites) or a PostHog host.
NEXT_PUBLIC_POSTHOG_HOST=/ingest
